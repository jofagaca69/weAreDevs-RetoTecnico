import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jofagaca69.wadfrontend',
  appName: 'wadfrontend',
  webDir: 'dist/wad-frontend/browser',
};

export default config;
